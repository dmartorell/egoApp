{
  "description": "Shared types and utilities for EgoApp",
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/jest": "^29.5.8",
    "@types/node": "^20.10.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "yarn": ">=1.22.0"
  },
  "main": "dist/index.js",
  "name": "@egoapp/shared",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist .turbo node_modules/.cache",
    "dev": "tsc --watch",
    "lint": "eslint 'src/**/*.{js,ts}' --max-warnings 0",
    "lint:check": "eslint 'src/**/*.{js,ts}' --max-warnings 0",
    "lint:fix": "eslint 'src/**/*.{js,ts}' --fix",
    "test": "jest",
    "type-check": "tsc --noEmit"
  },
  "types": "dist/index.d.ts",
  "version": "0.1.0"
}
